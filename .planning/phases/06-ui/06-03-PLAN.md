---
phase: 06-ui
plan: 03
type: execute
wave: 3
depends_on:
  - 06-02
files_modified:
  - prompt_tool/app.py
  - prompt_tool/app_controller.py
  - tests/test_generator_v2.py
autonomous: true
requirements:
  - UI-03
  - UI-04
  - QA-03
user_setup: []

must_haves:
  truths:
    - "结果界面右侧显示可收起/展开的优化侧栏面板"
    - "优化面板包含「追加要求」多行输入框、「换风格」下拉菜单、「加限制」多选复选框"
    - "用户提交优化请求后重新生成全部三张策略卡"
    - "优化面板默认展开，点击收起按钮后面板隐藏，三卡区域扩展占满"
    - "用户可点击 Header 上的「📚 知识包」按钮查看当前行业知识包概览"
    - "金融和制造业的生成结果中显示合规声明提示"
    - "合规声明以醒目横幅形式显示在结果界面顶部"
  artifacts:
    - path: "prompt_tool/app.py"
      provides: "优化侧栏面板（_build_optimization_panel）、知识包概览弹窗（_on_show_knowledge_pack_v4）、合规声明横幅（compliance_banner）"
    - path: "prompt_tool/app_controller.py"
      provides: "refine_prompts 方法，接收追加要求+风格+限制参数重新生成"
    - path: "tests/test_generator_v2.py"
      provides: "TestComplianceDisclaimer 类，验证金融/制造行业提示词包含合规声明"
  key_links:
    - from: "prompt_tool/app.py (优化面板「🔄 重新生成」按钮)"
      to: "prompt_tool/app_controller.py (refine_prompts)"
      via: "AppController.refine_prompts(追加要求, 风格, 限制列表)"
      pattern: "self\\._v4_controller\\.refine_prompts"
    - from: "prompt_tool/app.py (知识包按钮)"
      to: "prompt_tool/knowledge_manager.py"
      via: "knowledge_manager.get_index() 获取索引数据"
      pattern: "knowledge_manager\\.get_index\\(\\)"
    - from: "prompt_tool/app.py (合规横幅)"
      to: "prompt_tool/app_controller.py (industry_id)"
      via: "检查 industry_id 是否为 'finance' 或 'manufacturing'"
      pattern: "industry_id.*in.*\\[.finance.manufacturing.\\]"
---

<objective>
## Phase Goal

**As a** 普通用户，**I want to** 通过对话式界面描述需求并获得三个可直接使用的 AI 提示词，**so that** 即使我不懂如何写提示词，也能快速获得高质量的 AI 指令。

### Plan 03: Optimization Panel + Knowledge Visibility + Compliance

**Purpose:** 完成 v4 界面的所有剩余功能 — 结果优化面板（用户可在生成后进一步细化 per UI-03）、知识包可见性（用户可查看行业知识包覆盖范围 per UI-04）、合规声明（金融/制造敏感行业提示词包含合规提示 per QA-03）。

**Output:** `prompt_tool/app.py`（优化面板 + 知识包弹窗 + 合规横幅）、`prompt_tool/app_controller.py`（refine_prompts 优化方法）、`tests/test_generator_v2.py`（合规声明单元测试）。
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-ui/06-CONTEXT.md (Claude's Discretion: 优化面板风格选项、合规声明触发条件/展示位置、知识包可见性展示方式)
@.planning/phases/06-ui/06-UI-SPEC.md (结果界面 Layout 含优化面板, OptimizationPanel 组件描述, Remaining Discretion)
@.planning/phases/06-ui/06-RESEARCH.md (Pattern 4: Optimization Side Panel Toggle, QA-03 compliance approach recommendation — Option C: UI banner)
@.planning/phases/06-ui/06-PATTERNS.md (Knowledge Manager Integration pattern, Colors, STRATEGIES)
@prompt_tool/knowledge_manager.py (knowledge_manager.get_index() 返回格式, get_loaded_industry_ids())
@prompt_tool/generator.py (PromptGeneratorV2 — generate_all() 返回格式, 知识注入点)
@prompt_tool/conversation_engine.py (generate_complete() API, _last_analysis 包含 industry_id)
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 合规声明 — QA-03 单元测试 + PromptGeneratorV2 合规注入</name>
  <files>tests/test_generator_v2.py, prompt_tool/generator.py</files>
  <read_first>
D:\文件\提示词工具\tests\test_generator_v2.py (现有测试 — 了解 fixture 和 mock 模式)
D:\文件\提示词工具\tests\conftest.py (共享 fixture — sample_analysis_result 等)
D:\文件\提示词工具\prompt_tool\generator.py (PromptGeneratorV2 类 — generate_all(), _filter(), self.industry_id)
  </read_first>
  <behavior>
    - Test 1: industry_id 为 "finance" 时，所有三个策略的生成结果中都包含"合规声明"或"风险提示"或"不构成"相关文字
    - Test 2: industry_id 为 "manufacturing" 时，所有三个策略的生成结果中都包含"合规"或"安全规范"或"国标"相关文字
    - Test 3: industry_id 为 "internet_it" 时，generate_all() 不崩溃且返回三个策略键
    - Test 4: 合规声明内容包含免责条款（如"不构成投资建议"或"以国家/行业标准为最终依据"）
  </behavior>
  <action>
实现 QA-03 合规声明功能：在 PromptGeneratorV2 中为敏感行业（金融 finance、制造 manufacturing）的生成结果追加合规声明。per RESEARCH.md QA-03 recommendation (Option C)：合规声明由生成器注入到输出中，同时在 UI 层以横幅形式展示。

### 1. PromptGeneratorV2 合规注入（generator.py）

在 PromptGeneratorV2 类中添加合规声明机制。

**添加合规声明常量（类级别或模块级别）：**
```python
COMPLIANCE_DISCLAIMERS = {
    "finance": (
        "\n\n---\n**⚖️ 合规声明：** 本提示词仅供专业人士参考。"
        "涉及金融建议、投资分析或财务规划的内容，请以持牌金融机构的正式意见为准。\n"
        "- 输出内容不构成投资建议\n"
        "- 涉及具体金融产品或策略时请标注风险等级\n"
        "- 遵守相关金融监管法规要求"
    ),
    "manufacturing": (
        "\n\n---\n**⚖️ 合规声明：** 本提示词仅供技术参考。"
        "涉及制造工艺、安全规范或质量控制的内容，请以国家/行业标准为最终依据。\n"
        "- 涉及安全操作时请注明安全规程编号\n"
        "- 产品规格请参考对应国标/行标\n"
        "- 遵守安全生产相关法律法规"
    ),
}
```

**添加方法 `_compliance_disclaimer(self) -> str | None`:**
- 检查 `self.industry_id` 是否在 `COMPLIANCE_DISCLAIMERS` 中
- 如果在，返回对应声明文本；否则返回 None

**修改 `generate_all()` 方法，在 _filter 之后追加合规声明：**
- 调用 `disclaimer = self._compliance_disclaimer()`
- 如果 disclaimer 不为 None，遍历 prompts dict 每个 key，追加到文本末尾

**注意：** industry_id 可能通过两种路径进入：
1. 通过 `generation_context.industry_id` (context_builder 提供的规范化 ID)
2. 通过 `analysis_result.industry_key` (v3.0 中文 key)
测试时需要覆盖两种 ID 格式（finance ↔ 金融，manufacturing ↔ 制造）。
`_compliance_disclaimer` 应同时检查 `self.industry_id` 和 `self.r.get("industry_key")`。

### 2. 合规测试（tests/test_generator_v2.py）

在现有测试文件中添加 `TestComplianceDisclaimer` 类，包含 3 个测试：

- `test_finance_prompts_contain_compliance`：industry_id="finance"，生成后检查所有三个策略包含合规关键字
- `test_manufacturing_prompts_contain_compliance`：industry_id="manufacturing"，检查合规关键字
- `test_non_sensitive_industry_no_crash`：industry_id="internet_it"，验证 generate_all() 正常返回不崩溃

测试创建 PromptGeneratorV2 实例时，mock 掉 `_task_knowledge` 和 `_anti_pattern_rules` 以绕过知识包加载依赖（测试环境可能无编译产物）：
```python
gen = PromptGeneratorV2(analysis, ctx)
gen._task_knowledge = {}
gen._anti_pattern_rules = []
result = gen.generate_all()
```
  </action>
  <verify>
    <automated>pytest tests/test_generator_v2.py::TestComplianceDisclaimer -x --tb=short</automated>
  </verify>
  <done>金融行业生成的三个策略提示词均包含合规声明（"合规声明"/"风险提示"/"不构成"之一）；制造行业生成的三个策略提示词均包含合规声明；互联网/IT 行业生成无崩溃且不强制合规声明；测试通过 pytest。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 优化侧栏面板 — 收起/展开 + 追加要求 + 换风格 + 加限制 + 重新生成</name>
  <files>prompt_tool/app.py, prompt_tool/app_controller.py</files>
  <read_first>
D:\文件\提示词工具\prompt_tool\app.py (当前状态 — result_frame, _result_cards, _populate_result_cards, _v4_controller, cards_area grid 结构)
D:\文件\提示词工具\prompt_tool\app_controller.py (当前状态 — process_answer, skip_and_generate, generate_complete, reset, callbacks)
D:\文件\提示词工具\.planning\phases\06-ui\06-UI-SPEC.md (优化面板交互规范: D-11/D-12/D-13)
D:\文件\提示词工具\.planning\phases\06-ui\06-RESEARCH.md (Pattern 4: Optimization Side Panel Toggle, _toggle_optimization_panel example)
D:\文件\提示词工具\.planning\phases\06-ui\06-PATTERNS.md (Colors palette, Thread Safety, Button Debounce)
D:\文件\提示词工具\prompt_tool\conversation_engine.py (generate_complete() — 理解重新生成的流程)
D:\文件\提示词工具\prompt_tool\session_manager.py (add_turn, get_active_session — 追加优化要求到会话中)
  </read_first>
  <behavior>
    - Test 1: 优化面板默认展开，result_frame 为两列布局（三卡区域+面板）
    - Test 2: 点击收起按钮后面板隐藏，三卡区域占据全部宽度
    - Test 3: 再次点击展开按钮后面板恢复
    - Test 4: 换风格下拉菜单包含："保持当前风格"、"更简洁"、"更详细"、"更专业"、"更通俗" 五个选项
    - Test 5: 重新生成期间按钮显示 disabled + "⏳ 优化中..." 文本
  </behavior>
  <action>
在结果界面右侧添加优化侧栏面板 per D-11/D-12/D-13。面板允许用户追加要求、切换风格、添加限制，然后重新生成全部三张策略卡。

### 1. 结果页面布局重构

修改 `_build_v4_result_page` 将 cards_area 变为两列布局：
- result_frame 新增 `grid_columnconfigure(0, weight=1)` (cards_area) + `grid_columnconfigure(1, weight=0)` (优化面板)
- cards_area 从单列改为 `grid(row=2, column=0, sticky="nsew")`（row=2 为合规横幅预留，见 Task 3）

### 2. 优化面板构建

在 `_build_v4_result_page` 末尾调用 `self._build_optimization_panel()`。

**方法 `_build_optimization_panel(self)`:**
创建 `self.opt_panel = ctk.CTkFrame(self.result_frame, fg_color="white", corner_radius=8, border_width=1, border_color=self.colors["border"], width=280)` — `grid(row=2, column=1, padx=(6,12), pady=6, sticky="nsew")` 并 `grid_propagate(False)`

面板内容从上到下 pack/grid：
A. **标题行**: "⚙️ 优化提示词" 标签 (CTkFont size=14, weight="bold")
B. **追加要求**: 标签 "📝 追加要求" + `self.refine_input = ctk.CTkTextbox(opt_panel, height=80, wrap="word", font=CTkFont(size=12), fg_color="#F8F9FA")`
C. **换风格**: 标签 "🎨 换风格" + `self.style_combo = ctk.CTkComboBox(opt_panel, values=["保持当前风格", "更简洁", "更详细", "更专业", "更通俗"], state="readonly")` per Claude's Discretion
D. **加限制**: 标签 "🔒 加限制" + 三个 CTkCheckBox（使用 tk.BooleanVar）：
   - "限制字数（≤500字）"
   - "包含示例/案例"
   - "纯文本（无表格/代码块）"
E. **重新生成按钮**: `self.opt_regenerate_btn = ctk.CTkButton(opt_panel, text="🔄 重新生成", fg_color=colors["primary"], height=36, command=self._on_opt_regenerate)` — 底部边距 12px

### 3. 面板收起/展开（D-13）

**Header 切换按钮**（在 result_frame header 右侧，与"返回对话""新对话"按钮同行）：
- `self.opt_toggle_btn = ctk.CTkButton(header, text="⚙️ 收起面板", font=CTkFont(size=12), fg_color="transparent", text_color="white", border_color="white", border_width=1, hover_color=darken, command=self._toggle_optimization_panel)`
- 面板可见时 text="⚙️ 收起面板"，隐藏时 text="◀ 展开面板"

**`_toggle_optimization_panel(self)`:**
```python
if self._panel_visible:
    self.opt_panel.grid_remove()
    # 让 cards_area 占满：移除 column 1 的约束
    self.result_frame.grid_columnconfigure(1, weight=0, minsize=0)
    self.opt_toggle_btn.configure(text="◀ 展开面板")
    self._panel_visible = False
else:
    self.opt_panel.grid()
    self.result_frame.grid_columnconfigure(1, weight=0, minsize=280)
    self.opt_toggle_btn.configure(text="⚙️ 收起面板")
    self._panel_visible = True
```
初始 `self._panel_visible = True` per D-13（默认展开）。

### 4. 重新生成逻辑（D-12）

**`_on_opt_regenerate(self)`:**
- 收集参数：`additional = self.refine_input.get("1.0", "end-1c").strip()`、`style = self.style_combo.get()`（"保持当前风格"视为 None）、`constraints = [cb.cget("text") for cb, var in self._opt_constraint_vars if var.get()]`
- 禁用按钮并启动后台线程 → `refine_prompts(additional, style, constraints)` → UI 线程回调 `_v4_on_refined` → `_populate_result_cards()` 更新三卡

### 5. AppController.refine_prompts（app_controller.py）

```python
def refine_prompts(self, additional_reqs="", style=None, constraints=None):
    constraints = constraints or []
    session = session_manager.get_active_session()
    
    # 记录优化输入到会话
    if additional_reqs:
        session_manager.add_turn("user", f"追加要求：{additional_reqs}", "answer")
    if style and style != "保持当前风格":
        session_manager.add_turn("user", f"风格偏好：{style}", "answer")
    if constraints:
        session_manager.add_turn("user", f"限制条件：{'、'.join(constraints)}", "answer")
    
    # 重建上下文并重新生成
    from .generator import PromptGeneratorV2
    from .context_builder import build_generation_context
    context = build_generation_context(session) if session else {}
    
    # 将优化参数融入上下文
    if additional_reqs:
        context["conversation_summary"] = context.get("conversation_summary", "") + f"\n追加要求：{additional_reqs}"
    if style and style != "保持当前风格":
        context.setdefault("confirmed_info", {})["tone"] = style
    if constraints:
        context.setdefault("confirmed_info", {})["constraints"] = constraints
    
    gen = PromptGeneratorV2(self._engine._last_analysis, context)
    return gen.generate_all()
```

**Decision coverage:** D-11 (右侧面板), D-12 (重新生成全部三卡), D-13 (可收起展开)
  </action>
  <verify>
    <automated>python -c "import prompt_tool.app; app = prompt_tool.app.PromptToolApp(); assert hasattr(app, 'opt_panel'); assert app._panel_visible; app._toggle_optimization_panel(); assert not app._panel_visible; print('Opt panel toggle OK')"</automated>
  </verify>
  <done>结果界面右侧显示优化面板（追加要求/换风格/加限制/重新生成按钮）；面板默认展开，可通过 header 按钮收起/展开；收起后三卡区域自动扩展；提交优化后三卡内容被新生成的提示词替换；重新生成期间按钮显示禁用状态。</done>
</task>

<task type="auto">
  <name>Task 3: 知识包可见性 + 合规横幅 UI + 最终集成</name>
  <files>prompt_tool/app.py</files>
  <read_first>
D:\文件\提示词工具\prompt_tool\app.py (当前状态 — 优化面板、result_frame、header、_v4_controller、knowledge_manager 已导入)
D:\文件\提示词工具\prompt_tool\knowledge_manager.py (get_index() 返回格式, get_loaded_industry_ids(), is_fallback_active())
D:\文件\提示词工具\prompt_tool\generator.py (PromptGeneratorV2 — _compliance_disclaimer from Task 1)
D:\文件\提示词工具\.planning\phases\06-ui\06-UI-SPEC.md (Remaining Discretion: 知识包可见性建议 Header 按钮 + 弹窗)
D:\文件\提示词工具\.planning\phases\06-ui\06-PATTERNS.md (Knowledge Manager Integration pattern)
  </read_first>
  <action>
实现 UI-04（知识包可见性）和 QA-03 合规横幅 UI 展示，并完成最终集成。

### 1. 知识包可见性弹窗（UI-04）

**对话页面 Header 知识包按钮**：在 `_build_v4_chat_page` 的 header 中激活 Plan 06-01 创建的占位按钮，移除 `state="disabled"`，设置 `command=self._on_show_knowledge_pack_v4`。

**结果页面 Header 知识包按钮**：在 `_build_v4_result_page` 的 header 中复制相同的知识包按钮（与"返回对话""新对话"按钮同行）。

**方法 `_on_show_knowledge_pack_v4(self)`:**
创建 `ctk.CTkToplevel` 弹窗（非 messagebox，更现代化），尺寸 500x450，`grab_set()` 模态。

弹窗布局（从上到下）：
- **Header**: 深蓝底色 frame，"📚 当前可用知识包" 标题 + 状态标签（"✅ v4.0 知识包已加载" 或 "⚠️ 使用 v3.0 兼容模式"）
- **ScrollableFrame**: 每个行业一张卡片（白底/当前行业浅蓝底高亮），显示：
  - 图标 + 行业名称（当前行业标注"← 当前"）
  - 统计：术语数/场景数/追问树数/角色数
  - 简介（截断至 80 字符）
- **关闭按钮**: 底部居中

数据来源：`knowledge_manager.get_index()` 返回 `{industry_id: {name, icon, description, stats: {term_count, scenario_count, tree_count, role_count}}}`

当前行业判断：`self._v4_controller._engine._last_analysis.get("industry_id")`（如果 `_v4_controller` 存在且有活跃 engine）。

### 2. 合规声明横幅 UI（QA-03）

**在 result_frame 中添加合规横幅（row=1，cards_area 之上）：**
```python
self.compliance_banner = ctk.CTkFrame(
    self.result_frame, fg_color="#FFF3CD", corner_radius=6,
    border_width=1, border_color="#FFC107"
)
self.compliance_banner.grid(row=1, column=0, columnspan=2, padx=12, pady=(4,0), sticky="ew")
self.compliance_banner.grid_remove()  # 默认隐藏

# 横幅内容（先构建文本标签，后续动态更新）
ctk.CTkLabel(self.compliance_banner, text="⚖️",
             font=ctk.CTkFont(size=16)).pack(side="left", padx=(12,6), pady=6)
self.compliance_label = ctk.CTkLabel(
    self.compliance_banner, text="",
    font=ctk.CTkFont(size=12), text_color="#856404",
    wraplength=700, justify="left"
)
self.compliance_label.pack(side="left", padx=(0,12), pady=6)
```

**合规文本映射（模块级常量）：**
```python
COMPLIANCE_BANNER_TEXTS = {
    "finance": "金融行业内容 — 本提示词仅供专业参考，不构成投资建议。涉及金融建议请以持牌机构正式意见为准。",
    "manufacturing": "制造行业内容 — 本提示词仅供技术参考。涉及安全规范请以国家/行业标准为最终依据。",
}
```

**在 `_populate_result_cards` 中集成合规横幅显隐逻辑：**
- 获取 `industry_id = self._v4_controller._engine._last_analysis.get("industry_id")` if controller exists
- 同时在 `self.generated_prompts` 的文本中后向检查 "合规声明" 关键字（兼容 v3 模式 generate_complete 路径）
- 如果 industry_id in ("finance", "manufacturing")：显示横幅，更新文本；否则隐藏
- 使用 `compliance_label.configure(text=COMPLIANCE_BANNER_TEXTS.get(industry_id, ""))` 更新文本

### 3. 布局调整

修改 `_build_v4_result_page` 的 grid 结构以容纳合规横幅：
- row=0: header（固定）
- row=1: compliance_banner（固定，默认隐藏）— 新增
- row=2: cards_area (column=0) + opt_panel (column=1)（weight=1 扩展行）
- result_frame.grid_rowconfigure(2, weight=1)

### 4. 最终集成检查

- [ ] 对话页面 Header "📚 知识包" 按钮 → 弹窗显示所有行业
- [ ] 结果页面 Header "📚 知识包" 按钮 → 弹窗显示所有行业（高亮当前行业）
- [ ] 金融行业结果 → compliance_banner 可见 + 横幅文本正确
- [ ] 制造行业结果 → compliance_banner 可见 + 横幅文本正确
- [ ] 其他行业结果 → compliance_banner 隐藏
- [ ] 优化面板重新生成后 → 合规横幅正确更新（敏感行业→显示，非敏感→隐藏）
- [ ] 现有 `_on_show_knowledge_pack()` (v3 messagebox) 保持可用
  </action>
  <verify>
    <automated>python -c "import prompt_tool.app; app = prompt_tool.app.PromptToolApp(); assert hasattr(app, 'compliance_banner'); assert hasattr(app, 'compliance_label'); print('Compliance banner + Knowledge popup UI OK')"</automated>
  </verify>
  <done>对话和结果页面 Header 均有知识包按钮，点击弹出 CTkToplevel 窗口显示所有行业+统计+当前行业高亮；金融/制造行业结果页面显示黄色合规横幅，其他行业不显示；合规横幅在优化重新生成后正确更新。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User input (优化面板) → PromptGeneratorV2 | 追加要求文本作为字符串拼接到生成上下文 — 无代码执行路径 |
| Knowledge pack index → UI display | 编译的静态 JSON 索引 — 可信数据源，无注入路径 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-05 | Information Disclosure | knowledge_manager.get_index() | accept | 索引数据为公开知识包元数据，无敏感信息泄露风险 |
| T-06-06 | Tampering | 优化面板 regenerate 按钮 | mitigate | _is_generating 标志 + button state="disabled" 防抖 |
</threat_model>

<verification>
### End-to-End Verification
1. 启动应用 → v4 对话 → 输入 "帮我分析金融投资组合"
2. 完成对话（或用逃生门）→ 验证结果页面：
   - 黄色合规横幅 "金融行业内容 — ...不构成投资建议..."
   - 三卡提示词末尾含合规声明
3. 点击 "📚 知识包" → 验证弹窗显示行业列表、统计信息、当前金融行业高亮
4. 优化面板：输入 "增加风险分析维度" → 选择 "更专业" → 勾选 "限制字数" → 点击重新生成
5. 三卡更新 → 合规横幅保持显示
6. 收起面板 → 三卡扩展 → 展开面板 → 面板恢复
7. 返回对话 → 新对话 → 尝试制造业输入 → 验证合规横幅
</verification>

<success_criteria>
- [ ] QA-03 合规测试通过：test_compliance_disclaimer (3 tests)
- [ ] 优化面板默认展开，收起/展开切换正常
- [ ] 优化面板的重新生成按钮触发生成新 prompt 并更新三卡
- [ ] 知识包概览弹窗显示所有可用行业、统计信息、当前高亮行业
- [ ] 金融行业结果页面显示黄色合规横幅 + 提示词含合规声明
- [ ] 制造行业结果页面显示黄色合规横幅 + 提示词含合规声明
- [ ] 非敏感行业结果页面无合规横幅
- [ ] v3 界面知识包按钮仍可使用（messagebox 版本）
</success_criteria>

<output>
Create `.planning/phases/06-ui/06-03-SUMMARY.md` when done
</output>

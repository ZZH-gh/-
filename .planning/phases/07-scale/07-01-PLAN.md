---
phase: 07-scale
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prompt_tool/knowledge_packs/02-sales-retail.yaml
autonomous: true
requirements: [KNOW-05]
tags: [knowledge-pack, sales-retail, content-authoring]

must_haves:
  truths:
    - "02-sales-retail.yaml contains 60+ term definitions with category, definition, aliases, usage_context, and related_terms"
    - "02-sales-retail.yaml contains 10+ task scenarios with descriptions, typical_output, complexity, frequency, and follow_up_tree references"
    - "02-sales-retail.yaml contains 6+ roles with KPI definitions, pain_points, common_tasks, and responsibilities"
    - "02-sales-retail.yaml contains 3+ workflows with steps, decision_points, and failure_modes"
    - "02-sales-retail.yaml contains 5+ doc templates with sections, tone, and common_mistakes"
    - "02-sales-retail.yaml contains 10+ follow-up trees at depth 3-4 with unique node_id prefixes, matching task_type values to tasks[].follow_up_tree"
    - "02-sales-retail.yaml contains 5+ pain_points with why_happens, who_feels_it, typical_phrases, and what_not_to_do"
    - "build_packs.py compiles successfully with the new file present"
    - "Inline Pydantic validation confirms terms >= 60, tasks >= 10, follow_up_trees >= 2"
  artifacts:
    - path: "prompt_tool/knowledge_packs/02-sales-retail.yaml"
      provides: "Complete sales/retail industry knowledge pack"
      min_lines: 2000
      contains: ["meta:", "terms:", "tasks:", "roles:", "workflows:", "docs:", "follow_up_trees:", "pain_points:"]
  key_links:
    - from: "tasks[].follow_up_tree"
      to: "follow_up_trees[].task_type"
      via: "exact string match"
      pattern: "tasks.*follow_up_tree.*== .*follow_up_trees.*task_type"
    - from: "roles[].common_tasks"
      to: "tasks[].name"
      via: "exact string match"
      pattern: "roles.*common_tasks.*tasks.*name"
---

<objective>
Create the complete sales/retail industry knowledge pack (02-sales-retail.yaml) per D-01~D-04, D-07~D-08, D-10.

Purpose: Deliver KNOW-05 — a production-quality knowledge pack covering retail stores, e-commerce, wholesale, customer management, and sales processes with 60+ terms, 10+ tasks, 10+ follow-up trees across the full 8-dimension schema.
Output: `prompt_tool/knowledge_packs/02-sales-retail.yaml` (~2500-3500 lines)
</objective>

<execution_context>
@C:/Users/29457/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/29457/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-scale/07-RESEARCH.md
@prompt_tool/knowledge_packs/01-internet-it.yaml
@prompt_tool/knowledge_packs/_schema.yaml
@build_packs.py

# RESEARCH.md relevant sections for this plan:
# - §Content Map: Sales/Retail (lines 174-235): term categories, task list, role list, tree outlines, pain points
# - §8-Dimension Schema Structure (lines 91-104): exact YAML structure order
# - §Follow-Up Tree Node Structure Pattern (lines 106-127): node field reference
# - §Code Examples (lines 543-733): term/role/workflow/tree patterns
# - §Common Pitfalls (lines 441-468): pitfalls 1-5 to avoid
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Write complete 02-sales-retail.yaml knowledge pack</name>
  <files>prompt_tool/knowledge_packs/02-sales-retail.yaml</files>

  <read_first>
    <item file=".planning/phases/07-scale/07-RESEARCH.md" reason="Sales/Retail content map: 60+ term definitions across 7 categories, 12 task scenarios, 6 role definitions, 10+ follow-up tree outlines, 5 pain points" sections="Content Map: Sales/Retail, Architecture Patterns, Code Examples, Common Pitfalls"/>
    <item file="prompt_tool/knowledge_packs/01-internet-it.yaml" reason="Authoritative structural template — copy the exact 8-section YAML structure, follow term/task/role/workflow/doc/tree/pain_point field conventions"/>
    <item file="prompt_tool/knowledge_packs/_schema.yaml" reason="Schema field reference — verify all required fields are present in each dimension"/>
  </read_first>

  <action>
Write `prompt_tool/knowledge_packs/02-sales-retail.yaml` as a complete, production-quality knowledge pack.

**File header:** Use YAML comment block matching the template style:
```
# ============================================
# 销售/零售 行业知识包 V1
# Schema 版本: 1.0 | 知识包版本: 1.0.0
# 覆盖场景：促销方案策划、销售数据分析、客户分群策略、渠道运营优化、商品选品分析、店铺运营报告、电商详情页文案、会员运营方案、销售话术设计、供应链优化方案、年度销售计划、竞品调研报告
# ============================================
```

**Section 1 — meta (per D-02 template structure):**
- `id: "sales_retail"` (snake_case, matches filename convention)
- `name: "销售/零售"`
- `icon: "🛒"` (quoted string — per template convention)
- `description:` multi-line covering retail stores, e-commerce, wholesale, customer management, sales processes
- `version: "1.0.0"`, `schema_version: "1.0"`, `last_updated: "2026-06-04"`

**Section 2 — terms (target: 60+, structured by category with YAML comments):**
Follow RESEARCH.md §Content Map: Sales/Retail §Terms. Write 60+ terms organized in these category groups (mark each group with `# --- category name ---` comments):

1. Sales metrics (10): 转化率, 客单价, 复购率, 客流量, 连带率, 成交率, 客诉率, 退货率, 毛利率, 坪效
2. E-commerce metrics (10): GMV, ROI, LTV, CAC, 客件数, 渗透率, 购物车放弃率, AOV, 流量成本, 点击率(CTR)
3. CRM terms (8): RFM分析, 线索评分, 客户分群, 客户生命周期, 沉默客户, 流失预警, 精准营销, 私域流量
4. Supply chain/retail ops (8): 库存周转率, SCM, 缺货率, 动销率, 库龄, 安全库存, 订货点, 供应链协同
5. Marketing/promotion (8): 促销ROI, 满减, 秒杀, 裂变营销, KOL营销, 内容营销, 会员体系, 积分体系
6. Sales process (6): 销售漏斗, 商机管理, 跟单, 回款, 渠道管理, 经销商管理
7. Retail operations (8): 巡店, 排班, 陈列(VMD), 补货, 盘点, 损耗率, 收银效率, 会员管理

**Each term MUST have all 6 fields (per template pattern, avoid Pitfall 1):**
```yaml
- term: "转化率"
  category: "销售指标"
  definition: "完成目标行为的用户数占进入流程总用户数的比例，衡量销售漏斗各环节转化效率的核心指标。电商场景中常指访问-下单转化率，门店场景中指进店-成交转化率。"  # 15-30 chars minimum
  aliases: ["Conversion Rate", "CVR", "转化"]  # 1-3 aliases
  usage_context: "销售经理和电商运营日常监控，用于识别销售漏斗瓶颈环节并制定优化策略"  # who uses it, when
  related_terms: ["销售漏斗", "客单价", "GMV"]  # 2-3 cross-references within this pack
```

**Section 3 — tasks (target: 10+, must match RESEARCH.md task list):**
Write the 12 tasks listed in RESEARCH.md §Tasks. Each task:
```yaml
- name: "促销方案策划"
  description: "针对特定商品或品类，设计包含促销目标、活动规则、预算分配、执行计划和效果预估的完整促销方案"  # detailed, not one-liner
  typical_output: "促销方案文档（含活动主题、目标客群、促销机制、预算明细、执行时间表、效果预估指标）"
  complexity: "medium"  # low/medium/high — use RESEARCH.md complexity values
  frequency: "high"  # low/medium/high/very_high
  follow_up_tree: "promo_planning"  # MUST match a follow_up_trees[].task_type exactly
```

Task names and their follow_up_tree keys:
1. 促销方案策划 → promo_planning
2. 销售数据分析 → sales_data_analysis
3. 客户分群策略 → customer_segmentation
4. 渠道运营优化 → channel_optimization
5. 商品选品分析 → product_selection
6. 店铺运营报告 → store_ops_report
7. 电商详情页文案 → product_page_copy
8. 会员运营方案 → member_ops_plan
9. 销售话术设计 → sales_script_design
10. 供应链优化方案 → supply_chain_opt
11. 年度销售计划 → annual_sales_plan
12. 竞品调研报告 → competitor_research

**Section 4 — roles (target: 6+, per RESEARCH.md):**
Roles from RESEARCH.md:
1. 销售经理 — KPIs: 销售额完成率, 回款率, 客户增长率
2. 电商运营 — KPIs: GMV达成率, 转化率, 流量留存率
3. 客户成功经理 — KPIs: 复购率, 客户满意度, 流失率
4. 品类采购经理 — KPIs: 缺货率, 毛利率, 供应商准时率
5. 零售店长 — KPIs: 坪效, 客单价, 损耗率
6. 市场/增长经理 — KPIs: 获客成本(CAC), 活动ROI

Each role MUST follow the template role pattern (3 KPIs with name/description/benchmark, 3-5 pain_points strings, 4-6 common_tasks matching task names, 4-5 responsibilities strings). See RESEARCH.md §Role Structure Pattern and §Code Examples for exact format.

**Section 5 — workflows (target: 3+):**
Create at least 3 workflows covering sales/retail core processes. Suggestions:
1. 促销活动执行流程 (category: 销售运营) — from planning to post-mortem
2. 客户生命周期管理流程 (category: 客户管理) — from acquisition to retention
3. 门店日常运营流程 (category: 门店管理) — opening, mid-day, closing routines
Each workflow: 5-8 steps (step/number, name, owner, deliverables), 1-2 decision_points, 2-3 failure_modes. See RESEARCH.md §Workflow Structure Pattern and §Code Examples.

**Section 6 — docs (target: 5+):**
Create doc templates for at least 5 task types. Each: name, task_type (matches a task name), 4-6 sections (title + prompt_hint), tone string, 3-4 common_mistakes. See RESEARCH.md §Doc Template Structure Pattern and template file for format.

**Section 7 — follow_up_trees (target: 10+, MUST be depth 3-4 per Pitfall 2):**
Create 10+ follow-up trees, one per task. Each tree MUST:
- Use unique node_id prefix per tree (e.g., `promo_q1`, `sales_q1`, `cust_q1` — avoid Pitfall 5)
- Follow the node structure from RESEARCH.md §Follow-Up Tree Node Structure Pattern
- Have depth >= 3 (avoid Pitfall 2 — trees that stop at depth 2 are rejected)
- Leaf nodes MUST be multi_choice (collecting output format preferences) or text_input, with `children: {}`, `fallback_node_id: null`, `is_leaf: true`
- `task_type` MUST exactly match the corresponding `tasks[].follow_up_tree` value
- Each tree MUST have `root_node_id` that exists in `nodes`

Tree outlines from RESEARCH.md:
1. promo_planning: 促销类型(满减/秒杀/裂变/会员专享) → 目标客群 → 预算范围 → 产出格式(leaf)
2. sales_data_analysis: 分析维度(客户/产品/渠道/时间) → 数据范围 → 分析深度 → 产出格式(leaf)
3. customer_segmentation: 分群目的(留存/激活/促活/交叉销售) → 数据来源 → 分群方法(RFM/行为/画像) → 产出格式(leaf)
4. channel_optimization: 渠道类型(线上/线下/私域) → 当前痛点 → 优化方向 → 产出需求(leaf)
5. product_selection: 选品场景(新品引入/季末汰换/大促选品) → 数据维度 → 评估标准 → 产出格式(leaf)
6. store_ops_report: 报告类型(日报/周报/月报) → 关注维度 → 数据范围 → 产出格式(leaf)
7. product_page_copy: 商品类型(标品/非标品/服务) → 目标客群 → 文案风格 → 产出内容(leaf)
8. member_ops_plan: 运营目标(拉新/促活/留存/转化) → 会员层级 → 触达渠道 → 方案范围(leaf)
9. sales_script_design: 销售场景(电销/面销/在线客服) → 客户类型 → 产品复杂度 → 产出格式(leaf)
10. supply_chain_opt: 优化方向(库存/物流/采购) → 当前指标 → 优化目标 → 产出需求(leaf)
11. annual_sales_plan: 计划范围(公司/部门/区域) → 时间维度(年度/季度分解) → 增长策略 → 产出格式(leaf)
12. competitor_research: 竞品类型(直接/间接/替代品) → 分析深度 → 对比维度 → 产出格式(leaf)

For each tree, write 4-5 nodes following the template convention. Depth-1 nodes use single_choice with 3-4 substantive options. Depth-2/3 nodes use single_choice or text_input. Leaf nodes use multi_choice for output format selection.

**Section 8 — pain_points (target: 5+):**
Write the 5 pain points from RESEARCH.md §Pain Points:
1. 客户流失严重
2. 库存积压
3. 渠道冲突
4. 促销ROI不透明
5. 客户数据孤岛

Each pain point MUST have all 6 fields per schema: name, description, why_happens, who_feels_it, typical_phrases (3-5 items), what_not_to_do.

**Quality rules (per RESEARCH.md Common Pitfalls):**
- Pitfall 1: Every term definition >= 15 Chinese characters. Include usage_context and 2-3 related_terms.
- Pitfall 2: Every follow-up tree has depth >= 3 (3-4 levels). No "flat" trees.
- Pitfall 3: Cross-industry terms (转化率, ROI, etc.) must have sales/retail-specific context in definition and usage_context.
- Pitfall 4: All required Schema fields present — especially is_leaf, fallback_node_id on every tree node.
- Pitfall 5: Unique node_id prefix per tree. No two trees share `n1` as root.
  </action>

  <verify>
    <automated>
      python -c "import sys; sys.path.insert(0,'.'); from build_packs import KnowledgePack; import yaml; data=yaml.safe_load(open('prompt_tool/knowledge_packs/02-sales-retail.yaml',encoding='utf-8')); pack=KnowledgePack(**data); print(f'OK: {len(pack.terms)} terms, {len(pack.tasks)} tasks, {len(pack.follow_up_trees)} trees, {len(pack.roles)} roles, {len(pack.workflows)} workflows, {len(pack.docs)} docs, {len(pack.pain_points)} pain_points'); assert len(pack.terms) >= 60, f'Terms: {len(pack.terms)} < 60'; assert len(pack.tasks) >= 10, f'Tasks: {len(pack.tasks)} < 10'; assert len(pack.follow_up_trees) >= 2, f'Trees: {len(pack.follow_up_trees)} < 2'"
    </automated>
  </verify>

  <done>
02-sales-retail.yaml passes Pydantic KnowledgePack model validation with terms >= 60, tasks >= 10, follow_up_trees >= 2. File follows the 8-dimension schema structure matching 01-internet-it.yaml template conventions.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Verify pack compiles with build_packs.py</name>
  <files>prompt_tool/knowledge_packs/02-sales-retail.yaml</files>

  <action>
Run the full build pipeline to verify the new pack compiles alongside existing packs:

```bash
python build_packs.py
```

This validates:
1. YAML syntax is correct
2. All Pydantic models pass (required fields, types, enums)
3. Follow-up tree cycle detection passes (no circular references)
4. JSON output is generated in knowledge_packs_compiled/

If build_packs.py reports any errors, fix the YAML file and re-run until it passes with "All packs compiled successfully."

After build passes, verify the compiled JSON exists and has correct stats:
```bash
python -c "import json; d=json.load(open('prompt_tool/knowledge_packs_compiled/sales_retail.json',encoding='utf-8')); print(f'Compiled: {len(d[\"terms\"])} terms, {len(d[\"tasks\"])} tasks, {len(d[\"follow_up_trees\"])} trees')"
```
  </action>

  <verify>
    <automated>python build_packs.py</automated>
  </verify>

  <done>
build_packs.py exits with code 0. Compiled JSON exists at prompt_tool/knowledge_packs_compiled/sales_retail.json. Index.json regenerated with sales_retail entry.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | This phase is pure static YAML content authoring — no runtime input boundaries |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | YAML file content | accept | Static data authored by developer, reviewed before commit. No user input reaches this file. |
| T-07-02 | Information Disclosure | Finance compliance language | mitigate | Finance pack (Plan 07-03) includes risk disclaimers and avoids absolute return promises per RESEARCH.md compliance notes. Other packs have no sensitive content. |
</threat_model>

<verification>
Run `python build_packs.py` — all packs compile. Inline Pydantic check confirms term/task/tree counts.
</verification>

<success_criteria>
- [ ] 02-sales-retail.yaml exists with 8 complete dimensions
- [ ] Terms >= 60 with definition, aliases, usage_context, related_terms per term
- [ ] Tasks >= 10 with non-trivial descriptions and follow_up_tree references
- [ ] Roles >= 6 with KPI objects (name/description/benchmark), pain_points, common_tasks, responsibilities
- [ ] Workflows >= 3 with steps, decision_points, failure_modes
- [ ] Docs >= 5 with sections (title/prompt_hint), tone, common_mistakes
- [ ] Follow-up trees >= 10 with depth >= 3, unique prefix per tree, task_type matching tasks
- [ ] Pain points >= 5 with all 6 schema fields
- [ ] build_packs.py succeeds with exit code 0
- [ ] All task[].follow_up_tree values match follow_up_trees[].task_type values exactly
</success_criteria>

<output>
Create `.planning/phases/07-scale/07-01-SUMMARY.md` when done
</output>
